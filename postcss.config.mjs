/**
 * PostCSS configuration used during styling builds.
 * This file tells the toolchain how to process the project CSS before it reaches the browser.
 */

const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
